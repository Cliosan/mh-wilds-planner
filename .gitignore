# dipendenze dei tool di build (rigenerabili con `npm install` in tools/)
tools/node_modules/
node_modules/

# config locale editor/agent
.claude/

# build offline a file singolo, generato da tools/build.js (rigenerabile)
mhwilds-standalone.html
